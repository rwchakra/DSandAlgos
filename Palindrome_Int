Problem - https://www.interviewbit.com/problems/palindrome-integer/

def isPalindrome(self, A):
        if (A < 0):
            return 0
    
        else:
        
            a = str(A)
        
            if(a[:] == a[::-1]):
                return 1
            else:
                return 0
